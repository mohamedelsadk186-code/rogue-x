PORT=5000
NODE_ENV=development
JWT_SECRET=change-me

# Comma-separated browser origins allowed to call the API (production frontend URL(s))
CLIENT_ORIGINS=https://your-app.onrender.com

# Google OAuth — must match frontend VITE_GOOGLE_CLIENT_ID (same Web Client ID).
# Also add this URI as Authorized JavaScript origin in Google Cloud for your site + localhost.
GOOGLE_CLIENT_ID=your-google-oauth-client-id.apps.googleusercontent.com
# Optional: iOS OAuth client id (helps verify tokens issued by iOS flows)
GOOGLE_IOS_CLIENT_ID=your-google-ios-client-id.apps.googleusercontent.com
# Optional: comma-separated list of additional OAuth client IDs to accept
GOOGLE_ALLOWED_AUDIENCES=

# Apple Sign In (server verification via apple-signin-auth)
# Use your Apple Services ID (client id) configured for Sign in with Apple
APPLE_CLIENT_ID=com.yourcompany.roguex.web

# Cloudinary uploads
CLOUDINARY_CLOUD_NAME=your-cloud-name
CLOUDINARY_API_KEY=your-api-key
CLOUDINARY_API_SECRET=your-api-secret
